#include <stdio.h>

int main() {
    int a = 10;
    int b = 3;
    int soma = a + b;
    int diferença = a - b;
    int produto = a * b;
    int quociente = a / b; // Note que a divisão é inteira

    printf("Soma: %d\n", soma);
    printf("diferença: %d\n", diferença);
    printf("produto: %d\n", produto);;
    printf("Quociente: %d\n", quociente);

    return 0;
}
